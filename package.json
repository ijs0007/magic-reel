{
  "name": "magic-reel",
  "version": "0.3.0",
  "private": true,
  "description": "Magic Reel — share film footage so cast & crew build their own reels. Each selected range is clipped from the master on Mux; the server joins the clips with an FFmpeg stream copy (no re-encoding).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "ffmpeg-static": "^5.2.0"
  }
}
